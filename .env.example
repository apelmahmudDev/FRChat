# Server-only REST API origin. Do not expose this variable to the browser.
CHAT_API_BASE_URL=https://frontend-task-chatapp.onrender.com/api

# Socket.IO must connect to the host root, not the REST /api path.
NEXT_PUBLIC_CHAT_SOCKET_URL=https://frontend-task-chatapp.onrender.com
